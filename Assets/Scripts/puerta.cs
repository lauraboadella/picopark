using UnityEngine;

public class puerta : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }


    
        void OnTriggerEnter2D(Collider2D other)
    {

        /*
        if (other.CompareTag("Player"))
        {

            // bool de si tiene combustible y other get component a variable dentro del script combustible/player?
            
            
            if (tieneCombustible)
            {
                //hacer que al presionar f los jugadores desaparezcan
                //poner pantalla de win
            }
            }
            */

        
    }
}
